spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demo_db?&useSSL=FALSE&allowPublicKeyRetrieval=TRUE
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    platform: mysql
  jpa:
    datasource: mysql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect